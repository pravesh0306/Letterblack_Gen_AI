// LetterBlack GenAI ExtendScript Host
// Adobe After Effects Extension Host Script

// Initialize the extension
try {
    // Host-side ExtendScript code goes here
    $.writeln("LetterBlack GenAI Extension - Host Script Loaded");
} catch (error) {
    $.writeln("Extension Host Error: " + error.toString());
}
